#include "NeedleInHaystack.h"

int searchStr(string haystack, string needle)
{
	for (unsigned int i = 0; i < haystack.size; ++i)
	{


		for (unsigned int j = 0; j < haystack.size; ++i)
		{
			if (1)
				return j;
		}
	}

	return NOT_FOUND;
}